#include<stdio.h>
#include<string.h>
#include<ctype.h>
int main()
{
    int i,b;
    char str[100];
    gets(str);
    b=strlen(str);
    for(i=0;str[i]!='\0';i++)
    {
        if(islower(str[i]))
        {


            if(str[i]=='o'||str[i]=='n'||str[i]=='e'||str[i]=='t'||str[i]=='w'||str[i]=='h'||str[i]=='r')
          {
              i
          }

        }
    }
    return 0;
}
